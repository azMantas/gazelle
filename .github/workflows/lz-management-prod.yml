name: lz-management-prod

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  landingzone:
    uses: ./.github/workflows/tmp-landingzone.yml
    with:
      topLevelManagementGroupName: gazelle
      environment: prod
      location: westeurope
      parameterFile: ./landingzones/parameters/cis-management-prod.json
    secrets: inherit